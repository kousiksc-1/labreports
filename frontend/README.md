# RAG Lab Chat Frontend

Dev:

```bash
npm install
npm run dev
```

Config:

- Create `.env.local` with:

```
VITE_API_BASE=http://127.0.0.1:8000
```

Build:

```bash
npm run build && npm run preview
```


